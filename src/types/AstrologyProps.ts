export interface AstrologyProps {
  // elementId: number;
  // modalityId: number;
  // rulerId: number;
  name: string;
  dates: string;
  // positiveTraits: string[];
  // negativeTraits: string[];
  image: string;
}
